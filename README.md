Before running the script please run the following commands.
Make sure you are in the correct directory and you are using the right Interpreter.

python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt

Run the app from the repo root:

python app.py
